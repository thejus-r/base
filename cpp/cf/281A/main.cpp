#include <bits/stdc++.h>
#include <cctype>
using namespace std;

void solve() {
    string s;
    cin >> s;

    s[0] = toupper(s[0]);

    cout << s;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();

    return 0;
}

#include <bits/stdc++.h>
using namespace std;

void solve() {
    int m, n;
    cin >> m;
    cin >> n;

    cout << (m * n) / 2;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();

    return 0;
}
